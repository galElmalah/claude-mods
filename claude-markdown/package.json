{
  "name": "claude-markdown",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Claude Code mod: a markdown viewer in its own pane, with notes you send back to Claude",
  "scripts": {
    "build:vendor": "node scripts/build-vendor.mjs",
    "test": "bun test tests/markdown.test.ts tests/notes.test.ts",
    "test:e2e": "bun test tests/e2e",
    "test:all": "bun test",
    "typecheck": "bunx -p typescript tsc -p . && bunx -p typescript tsc -p tsconfig.test.json",
    "validate": "claude plugin validate .claude-plugin/plugin.json && claude plugin validate ."
  },
  "devDependencies": {
    "@copilotkit/aimock": "^1.42.0",
    "@types/bun": "^1.4.2",
    "esbuild": "^0.25.0"
  }
}
