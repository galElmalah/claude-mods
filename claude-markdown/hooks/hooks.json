{
  "description": "claude-markdown: a markdown viewer in its own pane (/md <file>, or the View button on .md tool rows) with notes sent back to the prompt box. Needs CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1; builds without function hooks ignore the modules key.",
  "modules": [
    "./register.ts"
  ]
}
