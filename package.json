{
  "name": "claude-mermaid",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Claude Code mod: mermaid diagrams drawn inline in the transcript",
  "scripts": {
    "build:vendor": "node scripts/build-vendor.mjs",
    "test": "bun test tests/diagrams.test.ts",
    "test:e2e": "bun test tests/e2e",
    "test:all": "bun test",
    "typecheck": "bunx -p typescript tsc -p . && bunx -p typescript tsc -p tsconfig.test.json",
    "validate": "claude plugin validate .claude-plugin/plugin.json && claude plugin validate ."
  },
  "devDependencies": {
    "@copilotkit/aimock": "^1.42.0",
    "@types/bun": "^1.4.2",
    "beautiful-mermaid": "^1.1.3",
    "esbuild": "^0.25.0"
  }
}
